- flash.each do |name, msg|
  - if msg.present? 
    %br
    %br
    %div{ class: name.to_s != "error" ? "alert alert-success" : "alert alert-error"}
      = msg
      %a{ class: "close", "data-dismiss" => "alert"} ×